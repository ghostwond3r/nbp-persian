# A

apropos : جستجو در صفحات راهنمای راهنما 

apt-get : جستجو و نصب بسته های نرم افزاری

aptitude : جستجو و نصب بسته های نرم افزاری 

aspell : چک کننده ی  

awk : یافتن و جایگزینی متن، مرتب‌سازی/تأیید اعتبار/ایندکس پایگاه داده


# B 

basename : دایرکتوری و پسوند را از نام فایل ها جدا کنید

bash : می توان از Bourne Again Shell یا BASH به عنوان Bourne Shell ای نام برد که بصورت ویژه برای پروژه GNU طراحی شده است. 

bc : زبان ماشین حساب دقیق دلخواه

bg : ارسال به پس زمینه

break : خروج از یک حلقه

builtin :  یک پوسته داخلی را اجرا کنید

bzip2 : فشرده یا از حالت فشرده خارج کردن فایل(های) نامگذاری شده


# C

cal : نمایش یک تقویم

case : یک فرمان را به صورت مشروط انجام دهید

cat : محتوای فایل ها را به هم متصل کرده و چاپ (نمایش) می دهد

cd :  تغییر دایرکتوری

cfdisk : دستکاری جدول پارتیشن برای لینوکس

chgrp : تغییر مالکیت گروه

chmod : مجوزهای دسترسی را تغییر دهید

chown : مالک و گروه فایل را تغییر دهید

chroot : دستوری را با دایرکتوری ریشه متفاوت اجرا کنید

chkconfig : خدمات سیستم (سطح اجرا)

cksum : چاپ CRC چک مجموع و تعداد بایت

clear : صفحه ترمینال را پاک کنید

cmp : دو فایل را با هم مقایسه کنید

comm : دو فایل مرتب شده را خط به خط مقایسه کنید

command : یک فرمان را اجرا کنید - توابع پوسته را نادیده بگیرید

continue : تکرار بعدی یک حلقه را از سر بگیرید

cp : یک یا چند فایل را در مکان دیگری کپی کنید

cron : دیمون برای اجرای دستورات برنامه ریزی شده

crontab : دستوری را برنامه ریزی کنید تا در زمان دیگری اجرا شود

csplit : یک فایل را به قطعات تعیین شده در زمینه تقسیم کنید

cut : یک فایل را به چند قسمت تقسیم کنید


# D

date : تاریخ و زمان را نمایش داده یا تغییر دهید

dc : ماشین حساب میز

dd : تبدیل و کپی یک فایل، نوشتن هدر دیسک، بوت رکورد

ddrescue : ابزار بازیابی اطلاعات

declare : متغیرها را اعلام کرده و به آنها ویژگی بدهید

df : نمایش فضای آزاد دیسک

diff : نمایش تفاوت بین دو فایل

diff3 : نمایش تفاوت بین سه فایل

dig : جستجوی DNS

dir : فهرست مختصری از محتویات دایرکتوری

dircolors : تنظیم رنگ برای «ls»

dirname : یک نام مسیر کامل را به یک مسیر تبدیل کنید

dirs : نمایش لیست دایرکتوری های به خاطر سپرده شده

dmesg : پیام های هسته و درایور را چاپ کنید

du : تخمین استفاده از فضای فایل


# E

echo : نمایش پیام بر روی صفحه نمایش

egrep : جستجوی فایل(ها) برای خطوطی که با یک عبارت توسعه یافته مطابقت دارند

eject : رسانه قابل جابجایی را خارج کنید

enable : دستورات پوسته داخلی را فعال و غیرفعال کنید

env : متغیرهای محیطی

ethtool : تنظیمات کارت اترنت

eval : چندین دستور / آرگومان را ارزیابی کنید

exec : یک دستور را اجرا کنید

exit : از پوسته خارج شوید

expect : خودکار کردن برنامه های کاربردی دلخواه که از طریق ترمینال قابل دسترسی هستند 

expand : تب ها را به فاصله تبدیل کنید

export : یک متغیر محیطی را تنظیم کنید

expr : عبارات را ارزیابی کنید


# F

false : هیچ کاری انجام نده، ناموفق

fdformat : فرمت سطح پایین فلاپی دیسک

fdisk : دستکاری جدول پارتیشن برای لینوکس

fg : ارسال کار به پیش زمینه

fgrep : جستجوی فایل(های) برای خطوطی که با یک رشته ثابت مطابقت دارند

file : نوع فایل را تعیین کنید

find : فایل هایی را جستجو کنید که دارای معیارهای مورد نظر هستند

fmt : متن پاراگراف را دوباره فرمت کنید

fold : متن را به تناسب عرض مشخص بپیچید

for : کلمات را گسترش دهید و دستورات را اجرا کنید

format : دیسک ها یا نوارها را فرمت کنید

free : نمایش میزان مصرف حافظه

fsck : بررسی و تعمیر سازگاری سیستم فایل

ftp : پروتکل انتقال فایل

function : تعریف ماکروهای تابع

fuser : شناسایی/کشتن فرآیندی که به یک فایل دسترسی دارد


# G

gawk : یافتن و جایگزینی متن در فایل(ها)

getopts : پارامترهای موقعیتی را تجزیه کنید

grep : جستجوی فایل(های) برای خطوطی که با الگوی داده شده مطابقت دارند 

groupadd : افزودن یک گروه امنیتی کاربر

groupdel : حذف یک گروه

groupmod : یک گروه را اصلاح کنید

groups : چاپ نام گروه هایی که یک کاربر در آن است

gzip : فشرده یا از حالت فشرده خارج کردن فایل(های) نامگذاری شده


# H

hash : نام مسیر کامل یک آرگومان نام را به خاطر بسپارید

head : خروجی قسمت اول فایل(ها)

help : نمایش راهنمایی برای یک فرمان داخلی

history : تاریخچه فرمان

hostname : چاپ یا تنظیم نام سیستم


# I

iconv : مجموعه کاراکترهای یک فایل را تبدیل کنید

id : شناسه کاربر و گروه را چاپ کنید

if : یک فرمان را مشروط انجام دهید

ifconfig : یک رابط شبکه را پیکربندی کنید

ifdown : یک رابط شبکه را متوقف کنید

ifup : یک رابط شبکه را راه اندازی کنید

import : یک صفحه سرور X بگیرید و تصویر را برای نصب فایل ذخیره کنید

install : فایل ها را کپی کنید و ویژگی ها را تنظیم کنید


# J

jobs : فهرست کردن مشاغل فعال

join : خطوط را در یک فیلد مشترک به هم بپیوندید


# K

kill : توقف اجرای یک فرآیند

killall : کشتن فرآیندها با نام


# L

less : نمایش خروجی یک صفحه در یک زمان

let : انجام محاسبات بر روی متغیرهای پوسته

ln : یک پیوند نمادین به یک فایل ایجاد کنید

local : ایجاد متغیر

locate :  یافتن فایل ها

logname : چاپ نام ورود به سیستم فعلی

logout : از پوسته ورود خارج شوید

look : خطوطی را که با یک رشته معین شروع می شوند نمایش می دهد

lpc : برنامه کنترل چاپگر خط

lpr : چاپ آفلاین

lprint : یک فایل را چاپ کنید

lprintd : یک کار چاپ را لغو کنید

lprintq : صف چاپ را فهرست کنید

lprm : کارها را از صف چاپ حذف کنید

ls : فهرست اطلاعات مربوط به فایل(ها)

lsof : لیست فایل های باز


# M

make : یک گروه از برنامه ها را دوباره کامپایل کنید

man : دفترچه ی راهنما

mkdir : ایجاد پوشه(های) جدید

mkfifo : ساخت FIFO (لوله های نامگذاری شده)

mkisofs : ایجاد یک سیستم فایل هیبریدی ISO9660/JOLIET/HFS

mknod : فایل های مخصوص بلاک یا کاراکتر بسازید

more : نمایش خروجی یک صفحه در یک زمان

mount: یک سیستم فایل را نصب کنید

mtools : فایل های MS-DOS را دستکاری کنید

mtr : تشخیص شبکه (traceroute/ping)

mv : انتقال یا تغییر نام فایل ها یا دایرکتوری ها

mmv : انتقال انبوه و تغییر نام (فایل ها)


# N

netstat : اطلاعات شبکه

nice Set : اولویت یک دستور یا کار nl

number : خطوط و نوشتن فایل ها

nohup : دستوری را اجرا کنید که در برابر هنگوپ مصون است

notify-send : اعلان های دسکتاپ را ارسال کنید

nslookup : سرورهای نام اینترنتی را به صورت تعاملی جستجو کنید


# O

open :  یک فایل را در برنامه پیش فرض آن باز کنید

op : دسترسی اپراتور


# P

passwd : رمز عبور کاربر را تغییر دهید

paste : ادغام خطوط فایل ها

pathchk : قابلیت حمل نام فایل را بررسی کنید

ping : اتصال شبکه را تست کنید

pkill : جلوی اجرای فرآیندها را بگیرید

popd : بازیابی مقدار قبلی دایرکتوری فعلی 

pr : فایل ها را برای چاپ آماده کنید

printcap : پایگاه داده قابلیت چاپگر

printenv : متغیرهای محیط چاپ

printf : فرمت و چاپ داده ها

ps : وضعیت فرآیند

pushd : ذخیره کنید و سپس دایرکتوری فعلی را تغییر دهید

pwd : پرینت دایرکتوری در حال کار 


# Q

quota : استفاده از دیسک و محدودیت ها را نمایش دهید

quotacheck : یک سیستم فایل را برای استفاده از دیسک اسکن کنید

quotactl : سهمیه دیسک را تنظیم کنید


# R

ram : دستگاه دیسک رم

rcp : کپی فایل ها بین دو ماشین

read : خواندن یک خط از ورودی استاندارد

readarray : خواندن از stdin به یک متغیر آرایه فقط خواندنی

readonly : علامت گذاری متغیرها/توابع به عنوان فقط خواندنی

reboot : سیستم را راه اندازی مجدد کنید

rename : تغییر نام فایل ها

renice : تغییر اولویت فرآیندهای در حال اجرا

remsync :  فایل های راه دور را از طریق ایمیل همگام سازی کنید

return : خروج از یک تابع پوسته

rev : خطوط معکوس یک فایل

rm : حذف فایل ها

rmdir : حذف پوشه(ها)

rsync : کپی فایل از راه دور (همگام سازی درختان فایل)


# S

screen : ترمینال Multiplex، اجرای پوسته های راه دور از طریق ssh

scp :  کپی امن (کپی فایل از راه دور)

sdiff : ادغام دو فایل به صورت تعاملی

sed : ویرایشگر جریان

select : ورودی صفحه کلید را بپذیرید

seq : چاپ دنباله های عددی

set: دستکاری متغیرها و توابع پوسته

sftp : برنامه انتقال امن فایل

shift : پارامترهای موقعیتی را تغییر دهید

shopt : گزینه های پوسته

shutdown : خاموش کردن یا راه اندازی مجدد لینوکس

sleep : تاخیر برای مدت زمان مشخص

slocate : یافتن فایل ها

sort : مرتب سازی فایل های متنی

source : دستورات را از یک فایل «.» اجرا کنید

split : یک فایل را به قطعات با اندازه ثابت تقسیم کنید

ssh : Secure Shell client (برنامه ورود از راه دور) 

strace : ردیابی تماس ها و سیگنال های سیستم

su : جایگزینی هویت کاربر

sudo : یک فرمان را به عنوان کاربر دیگر اجرا کنید

sum : یک چک جمع برای یک فایل چاپ کنید

suspend : اجرای این پوسته را به حالت تعلیق درآورید

symlink : نام جدیدی برای یک فایل بسازید

sync : داده ها را روی دیسک با حافظه #T همگام سازی کنید


# T

tail : خروجی آخرین قسمت فایل

tar : بایگانی کننده نوار

tee : خروجی را به چندین فایل هدایت کنید

test : یک عبارت شرطی را ارزیابی کنید

time : زمان اجرای برنامه را اندازه گیری کنید

times : زمان کاربر و سیستم

touch : تمبرهای زمانی فایل را تغییر دهید

top : لیست فرآیندهای در حال اجرا در سیستم

traceroute: ردیابی مسیر به میزبان

trap : هنگامی که یک سیگنال تنظیم شده است، دستوری را اجرا کنید (بورن) 

tr : ترجمه، فشار دادن، و/یا حذف کاراکترها

true :  هیچ کاری انجام ندهید، با موفقیت

tsort : مرتب سازی توپولوژیکی

tty : نام فایل ترمینال را در stdin چاپ کنید

type : یک دستور را توصیف کنید


# U

ulimit : منابع کاربر را محدود کنید

umask : ماسک ایجاد فایل کاربران

umount : یک دستگاه را جدا کنید

unalias :  حذف یک نام مستعار

uname :  چاپ اطلاعات سیستم

unexpand : تبدیل فضاها به تب

uniq : یکپارچه سازی فایل ها

units : تبدیل واحدها از یک مقیاس به مقیاس دیگر

unset :  نام متغیرها یا تابع ها را حذف کنید

unshar :  اسکریپت های آرشیو پوسته را باز کنید

until : اجرای دستورات (تا خطا)

uptime : نمایش زمان کار

useradd : ایجاد حساب کاربری جدید

userdel : یک حساب کاربری را حذف کنید

usermod : تغییر حساب کاربری

users : لیست کاربرانی که در حال حاضر وارد شده اند

uuencode : یک فایل باینری را رمزگذاری کنید

uudecode : رمزگشایی فایل ایجاد شده توسط uuecode


# V

v : فهرست دقیق محتویات دایرکتوری (`ls -l -b)

vdir : فهرست دقیق فهرست فهرست مطالب ('ls -l -b')

vi : ویرایشگر متن

vmstat : گزارش آمار حافظه مجازی


# W


wait : منتظر بمانید تا یک فرآیند تکمیل شود

watch : اجرا/نمایش یک برنامه به صورت دوره ای

wc : چاپ تعداد بایت، کلمه و خطوط

whereis : مسیر $، man pages و فایل های منبع کاربر را برای یک برنامه جستجو کنید

which : مسیر $ کاربر را برای یک فایل برنامه جستجو کنید.

while :دستورات را اجرا کنید

who :تمام نام های کاربری که در حال حاضر وارد شده اید را چاپ کنید

whoami : شناسه و نام کاربری فعلی را چاپ کنید ('id -un')

wget : صفحات یا فایل های وب را از طریق HTTP، HTTPS یا FTP بازیابی کنید

write : برای کاربر دیگری پیام ارسال کنید


# X

xargs : اجرای ابزار، ارسال لیست(های) آرگومان ساخته شده

xdg-open : یک فایل یا URL را در برنامه مورد نظر کاربر باز کنید.